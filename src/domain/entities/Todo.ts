export class Todo {
    id: number
    name: string
    isShow: boolean

    constructor(id: number, name: string,isShow:boolean) {
        this.id = id
        this.name = name
        this.isShow = isShow

    }
}
